apply plugin: "war"

bootWar {
    mainClassName = "com.hoannguyen.app.KiotApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
